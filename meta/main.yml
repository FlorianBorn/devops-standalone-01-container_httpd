---
galaxy_info:
  role_name: container-httpd
  description: installs a containerized httpd service

dependencies:
  - role: podman
    src: git+https://github.com/geerlingguy/ansible-role-composer.git